<div class="page-header">
  <h1>Editar — <%= @company.name %></h1>
  <%= link_to company_path(@company), class:"btn-secondary" do %>
    <i class="bi bi-arrow-left"></i> Voltar
  <% end %>
</div>
<%= render "form", company: @company %>
